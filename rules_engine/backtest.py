# Placeholder de backtest. A implementar con series históricas reales.
async def run_backtest(rule_id: str, universe: list[str], params: dict):
    return {"rule_id": rule_id, "universe": universe, "params": params, "result": "TODO"}
